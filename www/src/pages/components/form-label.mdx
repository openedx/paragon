---
title: 'Form.Label'
type: 'component'
status: 'Stable'
designStatus: 'Done'
devStatus: 'Done'
notes: |

---

import SingleComponentStatus from '../../components/SingleComponentStatus';
import PropsTable from '../../components/PropsTable';

# Form.Label

<SingleComponentStatus frontmatter={props?.pageContext?.frontmatter} />

Provide a label for a form control.
`Form.Label` uses a `FormGroupContext` (provided by `Form.Group`) to ensure that
it has an htmlFor attribute matching the `Form.Control` according to WCAG guidelines.

### Basic usage

```jsx live
<Form.Group>
  <Form.Label>What kind of cats?</Form.Label>
  <Form.Control />
</Form.Group>
```

### Inline usage

```jsx live
<Form.Group className="d-flex align-items-center">
  <Form.Label isInline>What kind of cats?</Form.Label>
  <Form.Control />
</Form.Group>
```

### Sizes

```jsx live
<>
<Form.Group size="sm">
  <Form.Label>What kind of cats?</Form.Label>
  <Form.Control />
</Form.Group>
<Form.Group size="lg">
  <Form.Label>What kind of cats?</Form.Label>
  <Form.Control />
</Form.Group>
</>
```

### See Form.Control for floating labels

```jsx live
<Form.Group>
  <Form.Control floatingLabel="What kind of cats?" />
</Form.Group>
```

<PropsTable {...props.data.FormLabelMetadata} />

export const query = graphql`
  query {
    FormLabelMetadata: componentMetadata(displayName: { eq: "FormLabel" }) {
      ...ComponentDocGenData
    }
  }
`
