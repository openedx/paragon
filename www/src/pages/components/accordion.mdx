---
title: "Accordion"
type: "component"
status: "In Progress"
designStatus: "To Do"
devStatus: "To Do"
notes: 
---

import { StaticQuery, graphql } from 'gatsby';
import { Breadcrumb } from '~paragon-react';
import PropsTable from '../../components/PropsTable';

# Accordion

##### Basic Usage

```jsx live=true
<Accordion>
  hi
</Accordion>
```

##### Props

<StaticQuery
  query={graphql`query {
    componentMetadata(displayName: { eq: "Accordion" }) { ...ComponentDocGenData }
  }`}
  render={({ componentMetadata }) => {
    if (componentMetadata == null) return null;
    return <PropsTable propMetaData={componentMetadata.props} />;
  }}
/>
