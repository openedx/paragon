---
title: 'FormField'
type: 'component'
status: 'Stable'
designStatus: 'Done'
devStatus: 'Done'
notes: |

---

import get from 'lodash/get';
import SingleComponentStatus from '../../components/SingleComponentStatus';

# FormField

<SingleComponentStatus frontmatter={props?.pageContext?.frontmatter} />


##### Basic Usage

```jsx live
() => {
  const [value, setValue] = useState('');
  return (
  <>
  <Checkbox label="my select" />
  <CheckboxGroup options={[
    { label: 'Option 1', value: 'option 1 value', disabled: true },
    { label: 'Option 2', value: 'option 2 value', disabled: false }
  ]} label="my select" />

  </>
  );
}
```

```


  <Select label="my select">
    <option value="hi">hi</option>
    <option value="h2i">hi</option>
    <option value="h3i">hi</option>
  </Select>
  <Input label="my select" />
  <Textarea label="my select" />

<RadioGroup options={[]} label="my select" />
  <Switch label="my select" />
  <FormField
    label="First Name"
    name="fname"
    leadingIcon={<Icon src={Add} />}
    trailingIcon={<IconButton icon={FontAwesome.faTimes} alt='Close' onClick={() => {}} variant="primary" />}
    value={value}
    size="lg"
    onChange={(e) => setValue(e.target.value)}
  />
  <FormField
    label="First Name"
    name="fname"
    leadingIcon={<Icon src={Add} />}
    trailingIcon={<IconButton icon={FontAwesome.faTimes} alt='Close' onClick={() => {}} variant="primary" />}
    value={value}
    size="md"
    onChange={(e) => setValue(e.target.value)}
  />
  <FormField
    label="First Name"
    name="fname"
    leadingIcon={<Icon src={Add} />}
    trailingIcon={<IconButton icon={FontAwesome.faTimes} alt='Close' onClick={() => {}} variant="primary" />}
    value={value}
    size="sm"
    onChange={(e) => setValue(e.target.value)}
    type="textarea"
  />
  <InputSelect />
  <InputText />
  <InputTextarea />
  <InputRadioGroup />
    <InputRadio />
  <InputCheckbox />
  <InputCheckboxGroup />
  <InputSwitch />

  <FormField
    id="myid"
    label="My Label"
    helpText="Stuff" >> node
    errorText="Error stuff" >> node
    validText="Valid stuff" >> node
  >
    <InputText />
  </FormField>

```

- Normalize button vs icon
- Reconcile with validation form group
- pass through props to input
- make select dropdown icon look like others -> or add it automatically and hide original
- forward refs and props in the right places