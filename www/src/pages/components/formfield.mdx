---
title: 'FormField'
type: 'component'
status: 'Stable'
designStatus: 'Done'
devStatus: 'Done'
notes: |

---

import get from 'lodash/get';
import SingleComponentStatus from '../../components/SingleComponentStatus';

# FormField

<SingleComponentStatus frontmatter={props?.pageContext?.frontmatter} />


##### Basic Usage

```jsx live
() => {
  const [value, setValue] = useState('');
  return (
  <>
    <FormField>
      <FormFieldLabel>description</FormFieldLabel>
       <FormFieldControlDecoratorGroup
          leadingElement={<Icon src={Add} />}
          trailingElement={<Icon src={Add} />}
          floatingLabel="I'm a label"
        >
          <FormFieldControl value={value} onChange={(e) => setValue(e.target.value)} />
        </FormFieldControlDecoratorGroup>
      <FormFieldDescription>description</FormFieldDescription>
    </FormField>


    <FormField>
      <FormFieldLabel>description</FormFieldLabel>
       <FormFieldControlDecoratorGroup
          leadingElement={<Icon src={Add} />}
          trailingElement={<Icon src={Add} />}
          floatingLabel="I'm a label"
        >
          <FormFieldControl as="textarea" value={value} onChange={(e) => setValue(e.target.value)} />
        </FormFieldControlDecoratorGroup>
      <FormFieldDescription>description</FormFieldDescription>
    </FormField>



    <FormField>
      <FormFieldLabel>description</FormFieldLabel>
       <FormFieldControlDecoratorGroup
          leadingElement={<Icon src={Add} />}
          floatingLabel="I'm a label"
        >
          <FormFieldControl as="select" value={value} onChange={(e) => setValue(e.target.value)}>
            <option>hi</option>
            <option>hi</option>
            <option>hi</option>
            <option>hi</option>
          </FormFieldControl>
        </FormFieldControlDecoratorGroup>
      <FormFieldDescription>description</FormFieldDescription>
    </FormField>
  </>
  );
}
```

```

<TextField
      className="mb-3"
      leadingElement={<Icon src={Add} />}
      trailingElement={<Icon src={Add} />}
      label="Last Name"
    />

    <TextField
      as="textarea"
      className="mb-3"
      leadingElement={<Icon src={Add} />}
      trailingElement={<Button size="sm" variant="tertiary">Copy</Button>}
      label="Last Name"
    />

    <SelectField
      className="mb-3"
      leadingElement={<Icon src={Add} />}
      trailingElement={<Button>Copy</Button>}
      label="Last Name"
    >
      <option>1</option>
      <option>2</option>
      <option>3</option>
      <option>4</option>
      <option>5</option>
    </SelectField>










<InputText className="mb-3" size="lg" />
    <InputText className="mb-3" size="sm" />

    <InputSelect className="mb-3" size="lg" />
    <InputSelect className="mb-3" />
    <InputSelect className="mb-3" size="sm" />

    <InputTextarea className="mb-3" size="lg" />
    <InputTextarea className="mb-3" />
    <InputTextarea className="mb-3" size="sm" />

    <InputTextarea className="mb-3" size="lg" />
    <InputTextarea className="mb-3" />
    <InputTextarea className="mb-3" size="sm" />

    <InputCheckbox className="mb-3" />
    <InputRadio className="mb-3" />
    <InputSwitch className="mb-3" />



<InputGroup>
  <InputGroup.Prepend><Icon src={Add} /></InputGroup.Prepend>
  <Input
</InputGroup>

<FormField
  id="email"
  label="Email"
>
  <FormField.Label floating>Email address</FormField.Label>
  <Input type="email" />
  <FormHelperText>We'll never share your email.</FormHelperText>
</FormField>


  <Checkbox label="my select" />
  <CheckboxGroup options={[
    { label: 'Option 1', value: 'option 1 value', disabled: true },
    { label: 'Option 2', value: 'option 2 value', disabled: false }
  ]} label="my select" />

  <Select label="my select">
    <option value="hi">hi</option>
    <option value="h2i">hi</option>
    <option value="h3i">hi</option>
  </Select>
  <Input label="my select" />
  <Textarea label="my select" />

<RadioGroup options={[]} label="my select" />
  <Switch label="my select" />
  <FormField
    label="First Name"
    name="fname"
    leadingIcon={<Icon src={Add} />}
    trailingIcon={<IconButton icon={FontAwesome.faTimes} alt='Close' onClick={() => {}} variant="primary" />}
    value={value}
    size="lg"
    onChange={(e) => setValue(e.target.value)}
  />
  <FormField
    label="First Name"
    name="fname"
    leadingIcon={<Icon src={Add} />}
    trailingIcon={<IconButton icon={FontAwesome.faTimes} alt='Close' onClick={() => {}} variant="primary" />}
    value={value}
    size="md"
    onChange={(e) => setValue(e.target.value)}
  />
  <FormField
    label="First Name"
    name="fname"
    leadingIcon={<Icon src={Add} />}
    trailingIcon={<IconButton icon={FontAwesome.faTimes} alt='Close' onClick={() => {}} variant="primary" />}
    value={value}
    size="sm"
    onChange={(e) => setValue(e.target.value)}
    type="textarea"
  />
  <InputSelect />
  <InputText />
  <InputTextarea />
  <InputRadioGroup />
    <InputRadio />
  <InputCheckbox />
  <InputCheckboxGroup />
  <InputSwitch />

  <FormField
    id="myid"
    label="My Label"
    helpText="Stuff" >> node
    errorText="Error stuff" >> node
    validText="Valid stuff" >> node
  >
    <InputText />
  </FormField>

```


- Reconcile with validation form group <<
- Add layout effect to change padding of inputs.
- make select options an array.



- Normalize button vs icon