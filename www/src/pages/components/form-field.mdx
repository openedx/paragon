---
title: 'FormGroup'
type: 'component'
status: 'Stable'
designStatus: 'Done'
devStatus: 'Done'
notes: |

---

import get from 'lodash/get';
import SingleComponentStatus from '../../components/SingleComponentStatus';
import PropsTable from '../../components/PropsTable';

# FormGroup

<SingleComponentStatus frontmatter={props?.pageContext?.frontmatter} />

`FormGroup` and the suite of components outlined here are the foundation for the
to be developed, `TextField`, `SelectField`, and `TextareaField`.

FormGroup renders its children inside a context that provides ids and attributes
that enable these components to relate to each other according to WCAG guidelines.

##### Basic usage with floating label

```jsx live
() => {
  const [value, setValue] = useState('');
  return (
    <FormGroup>
      <FormControl
        leadingElement={<Icon src={FavoriteBorder} />}
        trailingElement={<Icon src={Verified} />}
        floatingLabel="What kind of cats?"
        value={value}
        onChange={(e) => setValue(e.target.value)}
      />
      <FormControl.Feedback>We answer the important questions here.</FormControl.Feedback>
    </FormGroup>
  );
}
```

##### Basic usage with external label

```jsx live
() => {
  const [value, setValue] = useState('');
  const handleChange = (e) => setValue(e.target.value);
  return (
    <FormGroup>
      <FormLabel>What kind of cats?</FormLabel>
      <FormControl
        value={value}
        onChange={handleChange}
        leadingElement={<Icon src={FavoriteBorder} />}
        trailingElement={<Icon src={Verified} />}
      />
      <FormControl.Feedback>
        We answer the important questions.
      </FormControl.Feedback>
    </FormGroup>
  );
}
```

##### Sizes

```jsx live
() => {
  const [value, setValue] = useState('');
  const handleChange = (e) => setValue(e.target.value);
  return (
    <>
      <FormGroup size="sm">
        <FormControl
          value={value}
          onChange={handleChange}
          floatingLabel="What kind of cats?"
        />
      </FormGroup>

      <FormGroup>
        <FormControl
          value={value}
          onChange={handleChange}
          floatingLabel="What kind of cats?"
        />
      </FormGroup>

      <FormGroup size="lg">
        <FormControl
          value={value}
          onChange={handleChange}
          floatingLabel="What kind of cats?"
        />
      </FormGroup>
    </>
  );
}
```

##### States

```jsx live
() => {
  const [value, setValue] = useState('');
  const handleChange = (e) => setValue(e.target.value);
  return (
    <>
      <FormGroup isInvalid>
        <FormLabel>What kind of cats?</FormLabel>
        <FormControl value={value} onChange={handleChange} />
        <FormControl.Feedback variant="invalid">
          You are incorrect!
        </FormControl.Feedback>
      </FormGroup>

      <FormGroup isValid>
        <FormLabel>What kind of cats?</FormLabel>
        <FormControl value={value} onChange={handleChange} />
        <FormControl.Feedback variant="valid">
          You are correct!
        </FormControl.Feedback>
      </FormGroup>

      <FormGroup>
        <FormLabel>What kind of cats?</FormLabel>
        <FormControl value={value} onChange={handleChange} />
        <FormControl.Feedback>
          Let me help you!
        </FormControl.Feedback>
      </FormGroup>

      <FormGroup>
        <FormLabel>What kind of cats?</FormLabel>
        <FormControl value={value} onChange={handleChange} />
        <FormControl.Feedback icon={<Icon src={WarningFilled} />}>
          Making it custom. Be very careful.
        </FormControl.Feedback>
      </FormGroup>

      <FormGroup>
        <FormLabel>What kind of cats?</FormLabel>
        <FormControl value={value} onChange={handleChange} />
        <FormControl.Feedback>
          More than one at a time!
        </FormControl.Feedback>
        <FormControl.Feedback variant="valid">
          This is okay sometimes.
        </FormControl.Feedback>
      </FormGroup>
    </>
  );
}
```


##### Input types

```jsx live
() => {
  const [value, setValue] = useState('');
  const handleChange = (e) => setValue(e.target.value);
  return (
    <>
      <FormGroup>
        <FormControl
          value={value}
          onChange={handleChange}
          leadingElement={<Icon src={FavoriteBorder} />}
          floatingLabel="What kind of cats?"
        />
      </FormGroup>

      <FormGroup>
        <FormControl
          as="textarea"
          value={value}
          onChange={handleChange}
          leadingElement={<Icon src={FavoriteBorder} />}
          floatingLabel="What kind of cats?"
        />
      </FormGroup>

      <FormGroup>
        <FormControl
          as="select"
          value={value}
          onChange={handleChange}
          leadingElement={<Icon src={FavoriteBorder} />}
          floatingLabel="What kind of cats?"
        >
          <option value="">Select a cat</option>
          <option>Persian cat</option>
          <option>British Shorthair</option>
          <option>Maine Coon</option>
          <option>Siamese cat</option>
        </FormControl>
      </FormGroup>

      <FormGroup>
        <FormControl
          type="password"
          value={value}
          onChange={handleChange}
          leadingElement={<Icon src={FavoriteBorder} />}
          floatingLabel="Password cats?"
        />
      </FormGroup>

      <FormGroup>
        <FormControl
          type="date"
          value={value}
          onChange={handleChange}
          leadingElement={<Icon src={FavoriteBorder} />}
          floatingLabel="When cats?"
        />
      </FormGroup>

      <FormGroup>
        <FormControl
          type="number"
          value={value}
          onChange={handleChange}
          leadingElement={<Icon src={FavoriteBorder} />}
          floatingLabel="How many cats?"
        />
      </FormGroup>

      <FormGroup>
        <FormControl
          type="time"
          value={value}
          onChange={handleChange}
          leadingElement={<Icon src={FavoriteBorder} />}
          floatingLabel="Time cats?"
        />
      </FormGroup>
    </>
  );
}
```



<PropsTable {...props.data.FormGroupMetadata} />
<PropsTable {...props.data.FormLabelMetadata} />
<PropsTable {...props.data.FormControlMetadata} />
<PropsTable {...props.data.formControlDecoratorGroupMetadata} />

export const query = graphql`
  query {
    FormGroupMetadata: componentMetadata(displayName: { eq: "FormGroup" }) {
      ...ComponentDocGenData
    }
    FormLabelMetadata: componentMetadata(displayName: { eq: "FormLabel" }) {
      ...ComponentDocGenData
    }
    FormControlMetadata: componentMetadata(displayName: { eq: "FormControl" }) {
      ...ComponentDocGenData
    }
    formControlDecoratorGroupMetadata: componentMetadata(displayName: { eq: "FormControlDecoratorGroup" }) {
      ...ComponentDocGenData
    }
  }
`


```

<TextField
      className="mb-3"
      leadingElement={<Icon src={Add} />}
      trailingElement={<Icon src={Add} />}
      label="Last Name"
    />

    <TextField
      as="textarea"
      className="mb-3"
      leadingElement={<Icon src={Add} />}
      trailingElement={<Button size="sm" variant="tertiary">Copy</Button>}
      label="Last Name"
    />

    <SelectField
      className="mb-3"
      leadingElement={<Icon src={Add} />}
      trailingElement={<Button>Copy</Button>}
      label="Last Name"
    >
      <option>1</option>
      <option>2</option>
      <option>3</option>
      <option>4</option>
      <option>5</option>
    </SelectField>
```






- Add layout effect to change padding of inputs.
- make select options an array.
