---
title: 'TableWrapper'
type: 'component'
status: 'New'
designStatus: 'TBD'
devStatus: 'TBD'
---

import { StaticQuery, graphql } from 'gatsby';
import get from 'lodash/get';
import { TableWrapper, TextFilter } from '~paragon-react';
import PropsTable from '../../components/PropsTable';
import SingleComponentStatus from '../../components/SingleComponentStatus';

# Table

<SingleComponentStatus
  frontmatter={
    get(props, 'pageContext.frontmatter') ? props.pageContext.frontmatter : null
  }
/>

##### unstyled

```jsx live
<TableWrapper
  isSelectable
  isSortable
  isFilterable
  data={[
    {
      name: 'Lil Bub',
      color: 'brown tabby',
      famous_for: 'weird tongue',
    },
    {
      name: 'Grumpy Cat',
      color: 'siamese',
      famous_for: 'serving moods',
    },
    {
      name: 'Smoothie',
      color: 'orange tabby',
      famous_for: 'modeling',
    },
    {
      name: 'Maru',
      color: 'brown tabby',
      famous_for: 'being a lovable oaf',
    },
    {
      name: 'Keyboard Cat',
      color: 'orange tabby',
      famous_for: 'piano virtuoso',
    },
    {
      name: 'Long Cat',
      color: 'russian white',
      famous_for:
        'being loooooooooooooooooooooooooooooooooooooooooooooooooooooong',
    },
    {
      name: 'Zeno',
      color: 'brown tabby',
      famous_for: 'getting halfway there'
    },
  ]}
  initialColumns={[
    {
      Header: 'Name',
      accessor: 'name',

    },
    {
      Header: 'Famous For',
      accessor: 'famous_for',
    },
    {
      Header: 'Coat Color',
      accessor: 'color',
    },
  ]}
  title="Famous Internet Cats"
  bulkActions={[
    {
      buttonText: 'Enroll',
      handleClick: (selectedRows) => console.log('Enrolling ', selectedRows),
    },
    {
      buttonText: 'Assign',
      handleClick: (selectedRows) => console.log('Assigning ', selectedRows),
    },
  ]}
  additionalColumns={[
    {
      id: 'action',
      Header: 'Action',
      // Proptypes disabled as this prop is passed in separately
      // eslint-disable-next-line react/prop-types
      Cell: ({ row }) => <Button variant="link" onClick={() => console.log("Assign", row)}>Assign</Button>,
    }
  ]}
  defaultColumnValues={{ Filter: TextFilter }}
/>
```

##### Props

<StaticQuery
  query={graphql`
    query {
      componentMetadata(displayName: { eq: "TableWrapper" }) {
        ...ComponentDocGenData
      }
    }
  `}
  render={({ componentMetadata }) => {
    if (componentMetadata == null) return null;
    return <PropsTable propMetaData={componentMetadata.props} />;
  }}
/>
