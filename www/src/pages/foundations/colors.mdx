---
title: Colors
---

import { graphql } from 'gatsby';
import ColorSystem from '../../components/colors/ColorSystem';

# Colors

<ColorSystem cssSelectors={props.data.allCssUtilityClasses.nodes} />

export const pageQuery = graphql`
{
  allCssUtilityClasses(
    filter: {declarations: {regex: "/color/"}, isUtility: {eq: true}},
    sort: {fields: selector, order: ASC}
  ) {
    nodes {
      selector
      declarations
    }
    distinct(field: selector)
  }
}
`;
