---
title: Brand-Icons
---

import { Link, graphql } from 'gatsby';
import get from 'lodash/get';
import { Icon } from '~paragon-react';
import IconsTable from '../../components/IconsTable';
import PropsTable from '../../components/PropsTable';
import * as IconComponents from '~paragon-icons';

## Brand Icons

Displays a brand SVG icon from `@edx/paragon/icons`. See the <Link to="/components/icon">Icon Component</Link> for more information.

```jsx live
// Included in this live jsx scope:
// import { BsApple, BsSpotify } from '@edx/paragon/icons';
<Icon src={BsApple} />
```

The icons below are sourced from <a href="https://icons.getbootstrap.com/">Bootstrap Icons</a>. They are designed to be sharp at 24px by 24px.

<br />

<IconsTable iconNames={Object.keys(IconComponents).filter((iconName) => iconName.startsWith('Bs'))} />
