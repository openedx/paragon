---
title: 'useWindowSize'
type: 'hook'
status: 'New'
designStatus: 'Done'
devStatus: 'Done'
notes: ''
---

import get from 'lodash/get';
import SingleComponentStatus from '../../components/SingleComponentStatus';
import { useWindowSize } from '~paragon-react'

# useWindowSize

<SingleComponentStatus
  frontmatter={
    get(props, 'pageContext.frontmatter') ? props.pageContext.frontmatter : null
  }
/>

#### Sample Usage

<p>Change the screen size to see the effect.</p>

```jsx live
() => {
  const { height, width } = useWindowSize();
  return (
    <p>
      Window height: {height}px<br/>
      Window width: {width}px
    </p>
  );
}
```
