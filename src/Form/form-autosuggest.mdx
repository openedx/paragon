---
title: 'Form.Autosuggest'
type: 'component'
components:
- FormCheckbox
- CheckboxControl
- FormCheckboxSet
categories:
- Forms
status: 'New'
designStatus: 'Done'
devStatus: 'Done'
notes: |

---

Descriptions content


## Controlled Standalone Usage

```jsx live
() => {
    const [data, setData] = useState({
        title: '',
    });
    const options = ['Green', 'Red', 'Blue', 'Black', 'White', 'Purple', 'Orange', 'Grey'];

  return (
    // <Form.Autosuggest
    //     type="text"
    //     name="org"
    //     readOnly={false}
    //     value={data.title}
    //     options={options}
    //     controlClassName="has-value"
    //     handleChange={
    //         (value) => setData(prevState => ({ data: { ...prevState.data, org: value } }))
    //     }
    //     floatingLabel="This is floating label"
    //     // placeholder="This is placeholder"
    //     hasFieldError="This is has field error"
    //     errorMessage="This is error message"
    //     helpMessage="This is help message"
    // />
    <Form.Autosuggest
        // disabled
        options={options}
        value={data.title}
        readOnly={false}
        handleChange={
            (value) => setData(prevState => ({ data: { ...prevState.data, org: value } }))
        }
        placeholder="This is placeholder"
        floatingLabel="This is floating label"
        // hasFieldError="This is has field error"
        // helpMessage="This is help message"
        // errorMessage="This is error message"
        name="org"
    />
  );
}
```
